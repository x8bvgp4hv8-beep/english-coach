# Роадмап Coachirinho

Задачи в порядке отдачи. Берётся самая верхняя незакрытая. У каждой — «Готово, когда»:
критерий, который проверяется фактически, а не на глаз.

## Правила этого проекта

Прочитать до первой правки — на этих четырёх вещах подрываются заново каждый раз.

1. **Контент правится только в `native/Sources/EnglishCoachCore/Resources/Languages/<lang>/`.**
   `web/public/content/` собирается из него скриптом и лежит в `.gitignore`: правка там
   исчезнет при следующей сборке. Имя файла несёт язык префиксом (`en-b1.json`), потому что
   SwiftPM расплющивает папку ресурсов в корень бандла и два `b1.json` там столкнутся.
2. **Проверять только `npm test` из `web/`.** Он сначала синхронизирует контент, потом гоняет
   тесты. `npx vitest run` проверяет то, что лежало в `public/content` раньше, и на свежей
   правке контента говорит неправду.
3. **Храповики затянуты в нуль и падают на любой новый долг:** каждая тема силлабуса обязана
   набрать `minExercises` упражнений; перевод не может просить слово, которого курс не
   показывал раньше; глава схемы v2 должна быть помечена **минимум четырьмя** темами и обязана
   заявить `canDo`; упражнения в уроке идут только вверх по лестнице (диалог → карточки →
   правило → узнавание → производство), а checkpoint — производство без подсказок.
4. **Клиент один — PWA в `web/`.** Swift в `native/` отстал сознательно (решение от 11.09.2026),
   и его тесты закрепляют прежнее поведение пула. Не считать их источником правды и не
   «приводить web в соответствие» с ними.

---

## 1. Режим «учить слова» — своя единица, а не обрывок фразы ✔

**Сделано 11.09.2026.** Замер `npm run measure:vocabulary`: EN A1 1385 единиц, A2 1036,
B1 1208, B2 504 — только слова, именные и предложные группы и формы глагола. Отбор держат
пять тестов, включая список настоящих карточек, которые обязаны отбраковываться
(`of one problem`, `the letter says`, `nobody rests`). Проверено живьём: «Тренировка → Слова»
выдаёт `a week before` — «за неделю до» с примером употребления.

Формальный критерий («≥95% осмысленных») оказался слабым: его проходил и первый, мягкий
фильтр, пропускавший `stops me guessing` и `were left unpainted`. Поэтому правило ужесточено
сверх критерия и выборки проверены глазами. IPA не добавлен: поля нет ни в одной из 15 000
карточек, руками это не разметить, а произношение уже даёт озвучка.


С этого начался разбор 11.09.2026, и закрыта пока половина: слова стали уровневыми (не `Hello`
на B1), но словами не стали. Замер карточек B1 (4660 штук): **4%** одиночные слова, 61%
коллокации из 2–3 слов, 27% обрывки из четырёх и больше, 6% целые предложения. 27 карточек
вообще с многоточием (`How long have you been…?`). Приложение это и не скрывает — подпись над
карточкой в `web/src/app/Player.tsx` говорит «НОВАЯ ФРАЗА».

Причина в нарезке: карточки сделаны из реплик диалога, поэтому получаются
`had left the tap running`, `the one where the light is on`, `too happy to care`. Как фраза для
повторения это работает, как единица для запоминания — нет: её нельзя ни вспомнить отдельно,
ни применить в другом предложении.

Нужен словарь уровня: отобрать из курса лексические единицы (слово или устойчивая коллокация),
отбросить обрывки и всё с многоточием, дать перевод, пример употребления и произношение —
поля `IPA` в контенте сейчас нет ни одного.

**Готово, когда:** в режиме слов доля осмысленных единиц (одиночное слово или устойчивая
коллокация) не ниже 95% по тому же замеру, что дал 4%; ни одной единицы с многоточием; замер
воспроизводится скриптом, а не глазами; `npm test` зелёный.

## 2. Тренажёр форм неправильных глаголов ✔

**Сделано 11.09.2026.** «Тренировка → Формы глагола»: показана первая форма, спрашиваются
вторая и третья, пачка по 15. Глаголы из контента — 35 карточек вида «break — broke — broken»
плюс таблица форм внутри разбора темы (оттуда `be`, `go`, `do`, которых в карточках нет).
Вариативные формы принимаются любым вариантом («was / were», «got / gotten»).

Возврат промаха проверен фактически, а не тестом: `cost` с ответом `costed` ушёл в конец
очереди и вернулся на 30-м шаге прохода. Разбор показывается по полям — видно, какая именно
форма не вышла.


Блок из 91 упражнения есть (`en-b1-irregular-verbs`, первая глава B1), но тренировки форм в нём
нет: все упражнения — перевод предложений и выбор варианта. Этот материал учат не так. Нужен
быстрый дрилл: показано `break` — введи `broke` и `broken`, пачками по 15–20, промахи
возвращаются чаще. Сейчас, чтобы потренировать одну форму, надо перевести целое предложение,
и проверяется заодно всё остальное.

**Готово, когда:** режим открывается с «Тренировки», принимает обе формы за один заход, промах
по форме возвращает глагол в этой же сессии; список глаголов берётся из контента, а не
захардкожен в компоненте; пройден живьём в браузере (не только тестом) — скриншот в отчёте.

## 3. Разбор темы доступен из урока, и разборы для A2 ✔

**Сделано 11.09.2026.** Под абзацем правила в уроке появилась ссылка «Полный разбор темы»;
урок за разбором не закрывается — проверено фактически: 8/16 → разбор → снова 8/16, правило
на экране. Практику в этом случае не предлагают: человек уже внутри занятия.

`en-theory-a2.json` — все 19 грамматических тем A2, 64 минуты чтения, 74 разобранные ошибки.
Тест «каждая тема уровня разобрана» больше не смотрит только на B1: он идёт по каждому пакету
разборов, поэтому наполненный уровень без разбора теперь падает. Проверено живьём на профиле
A2: занятие предложило Past Simple, разбор с двумя таблицами и четырьмя ошибками, за ним
27 упражнений.


Внутри урока теория — тот же абзац `info`, а готовый разбор той же темы лежит рядом и они друг
о друге не знают. Ссылка «полный разбор темы» из урока — дешёвая правка, а теория окажется под
рукой тогда, когда она нужна.

Разборы сейчас только для B1 (13 тем, `en-theory-b1.json`). A2 наполнен на 91 час, разборов нет.

**Готово, когда:** из упражнения `info` есть переход на разбор своей темы и возврат в урок без
потери места; `en-theory-a2.json` покрывает все грамматические темы A2 (не `a2-tema-*`), у
каждой таблица форм и не меньше трёх разобранных ошибок с «почему» — это уже проверяет тест
«каждая грамматическая тема B1 разобрана», его надо распространить на A2.

## 4. Разборы для B2 ✔

**Сделано 11.09.2026.** `en-theory-b2.json` — все 12 грамматических тем B2, 46 минут чтения,
48 разобранных ошибок. Тот же тест покрытия, что и для A2 с B1, теперь держит три уровня.
Проверено живьём на профиле B2: занятие предложило «Второе условие», разбор с таблицами и
разбором ошибок вида «If I was you» → «If I were you», за ним 27 упражнений.


B2 наполнен на 139 часов и тоже без разборов. Делать после A2: там больше тем и они тяжелее.

**Готово, когда:** тот же критерий, что у A2, но для B2.

## 5. Аудирование не должно давать фразы без контекста ✔

**Сделано 11.09.2026.** Замер `npm run measure:listening`: осталось 62% на A1, 63% на A2,
60% на B1, 55% на B2 — больше половины, как требовал критерий. Отсеялись именно обрывки
реплик: «By this policy.», «Two years in a warehouse.», «Half on completion.»,
«Named, dated and linked.». Выборка из 20 оставшихся проверена глазами — все записываемы
на слух.

Порог в 4–5 слов, который стоял в постановке, оказался неверным решением: он выбрасывал
хорошие короткие фразы («It is cold.», «Who sang first?») и ронял A1 до 28%. Обрывки ловятся
не длиной, а началом (сочинительный союз или предлог в первой позиции) и отсутствием
сказуемого.


Фильтр пропускает всё, что начинается с заглавной и кончается точкой, поэтому в набор попадают
`By this policy.`, `And fifteen years of company.`, `By phone or email?` — записать такое на слух
вне разговора нельзя. Порог надо поднять: не меньше 4–5 слов, и в предложении должны быть
подлежащее и глагол.

**Готово, когда:** замер на контенте показывает, сколько предложений отсеялось и что осталось;
в оставшихся вручную проверена выборка из 20 — все записываемы на слух; в наборе осталось
достаточно материала (не меньше половины прежнего), иначе раздел опустеет.

## 6. Независимая проверка, что приложение учит ◐

**Инструмент сделан 11.09.2026, первый замер за Кристианом.** Банк `en-checkup-b1.json` —
20 переводов по 14 темам уровня. Непересечение с курсом проверено скриптом по всем
упражнениям всех пяти уровней и держится тестом, а декодер отказывается принять задание,
совпавшее с упражнением, — проверка живёт в коде, а не во внимательности автора банка.

Результат хранится своим списком (`state.checkups`) с датой и на «Прогрессе» идёт отдельной
секцией впереди остальных цифр, со сдвигом в процентных пунктах против прошлого раза.
Попытки и интервальные повторения от среза не пишутся: смешать замер с тренировкой значило
бы вернуть ту же замкнутую метрику. Проверено фактически: после прохода по экрану в базе
0 попыток, брошенный срез не записывается.

**Не закрыто:** «первый срез пройден живьём». По прямому указанию Кристиана срез за него не
проходился — замер имеет смысл только на живом человеке. Пункт останется открытым до первого
настоящего прохода.


Сейчас проценты считаются по тем же упражнениям, которые приложение и выдаёт: увидел
`My bike was stolen` двадцать раз, ответил верно — «85% по пассиву». Это замкнутый круг, и
цифра ничего не говорит о языке за пределами приложения.

Нужен контрольный срез **вне курса**: около 20 предложений, которых нет ни в одном упражнении,
на темы уровня, проходится раз в месяц. Тогда видно движение, а не привыкание к формулировкам.

**Готово, когда:** банк среза не пересекается с курсом (проверено скриптом по всем упражнениям
всех уровней); результат сохраняется с датой и показывается как отдельная линия, не смешанная
с процентами по упражнениям; первый срез пройден живьём.

## 7. Решить судьбу Swift-клиента ✔

**Сделано 11.09.2026: `native/` снят с роли клиента** (решение Кристиана). Записано в
`native/ARCHIVED.md` — а не в корневой `README.md`, потому что тот лежит в рабочем дереве с
незакоммиченными правками Кристиана и трогать его было нельзя.

Три утверждения, описывавших признанное ошибкой поведение («уровни ниже открыты целиком»,
«старые ошибки идут вторыми» в практике и в речи), **удалены** из Swift-тестов: тест, который
проходит и при этом описывает ошибку, вводит в заблуждение сильнее отсутствующего. В начале
файла тестов стоит пометка об архиве.

Заодно исправлена поломка, которую внесли разборы тем: загрузчик Swift читал все JSON языка
как курсы и падал на отсутствующем ключе `chapters` — 16 тестов из 16 не проходили. Список
`notCourses` теперь повторяет тот, что в `sync-content.mjs`. После правки `swift build`
проходит, `swift run EnglishCoachCoreTests` — «All core tests passed».


`native/` отстал на четыре правки ядра, и его тест `EnglishCoachCoreTests/main.swift` до сих
пор утверждает прежнее правило пула — то самое, из-за которого на B1 выдавался `hello`.
Противоречие в репозитории будет путать любого, кто откроет проект (включая меня в новой
сессии). Либо догнать четыре правки, либо явно снять с `native/` роль клиента.

**Готово, когда:** принято одно из двух решений и записано в `README.md`; если догонять — тесты
Swift проходят и поведение совпадает с web на тех же данных; если снимать — тесты, утверждающие
неверное, удалены или помечены устаревшими, а не оставлены «на всякий случай».

## 8. Мелочи, которые видно каждый раз ✔

**Сделано 11.09.2026.** Подпись над карточкой теперь считается по её содержимому:
«ФОРМЫ ГЛАГОЛА» над `get — got — got`, «НОВОЕ СЛОВО» над одиночным словом, «НОВАЯ ФРАЗА»
над куском реплики — тем же правилом, что отбирает слова в свой режим. Проверено живьём:
`roughly` и `obviously` подписаны словом, `a first call` и `the same day` — фразой.

В «Прогрессе» появилась секция «Разборы»: минуты за неделю и сколько тем разобрано,
отдельно от уроков и повторений. Время пишется в свой дневник (`state.studySeconds`)
только когда сессия пришла из разбора.


- Подпись «НОВАЯ ФРАЗА» стоит над `get — got — got` — это формы, а не фраза.
- Занятие по разбору не видно в статистике отдельной строкой: неясно, сколько времени ушло
  в разборы против уроков.

**Готово, когда:** подпись карточки соответствует тому, что на ней; в «Прогрессе» занятия по
разборам считаются отдельно от уроков и повторений.

## 9. Ровный интерфейс: выравнивание и кнопки «назад» ✔

**Сделано 13.09.2026, замером в браузере на 390×844.**

Что нашлось в «Настройках» (цифры до → после):

| Строка | Было | Стало |
|---|---|---|
| обычная строка | 54px | 54px |
| «Уровень» (5 вариантов) | **83px** | 54px |
| «Откуда ты» (8 стран) | **286px** | 54px |
| «Откуда ты» с полем ввода | 58px | 54px + своя строка поля |
| заметка под группой | 346px @22 | 354px @18, как группы |
| `.level-size` | без бокового отступа | 16px, по колонке группы |
| значение справа | прижималось по-разному | у всех 35px от края |

Причина высоких строк: `.pills` из кита переносит варианты (`flex-wrap: wrap`), и строка
росла по их числу; раскладка при этом задавалась инлайн-стилями в шести местах по-разному.
Теперь ряд вариантов едет одной линией с прокруткой, а инлайн-стили из `Settings.tsx`
убраны в CSS. Ряды «Уровень», «Цель в день», «Час» влезают целиком, прокручивается только
«Откуда ты» — восемь стран.

Кнопки выхода: на всех восьми экранах второго уровня и во всех режимах путь назад есть, но
выглядел он по-разному — текстовые `✕` и `‹` в пяти файлах против SVG-иконок в срезе и
формах, плюс ярлыки «Выйти» / «Закрыть» / «Назад» вперемешку. Приведено к одному набору:
иконка `arrow-left` и ярлык «Назад» на экранах, иконка `close` и «Закрыть» в режимах.

Проверено: все строки 54px, края 18/18 на «Сегодня», «Прогрессе», «Темах», «Разборах»,
«Языках», «Настройках»; горизонтального перелива нет ни на одном экране. Карта на «Курсе»
занимает всю ширину намеренно — это полотно маршрута, а не съехавший блок.


Кристиан 13.09.2026: «в настройках строчки некоторые слишком высокие, типа в два ряда, надо
всё выровнять. Вообще весь интерфейс я бы сделал максимально ровно. Где нет кнопки назад —
везде сделай, где она нужна».

Идёт первым, хотя названо вторым: это то, что он увидит руками завтра, и работа обозримая,
а 3000 слов — несколько проходов подряд.

Смотреть не «на глаз», а замером в браузере: у одинаковых по смыслу строк должна совпадать
высота, отступы и выравнивание правого края. Начинать с «Настроек» — там жалоба конкретная.

**Готово, когда:** пройдены все экраны на 390×844 (`Сегодня`, `Курс`, `Тренировка`,
`Прогресс`, `Настройки`, `Темы`, `Разборы`, `Языки`, плеер, срез, формы глагола); найденные
неровности выписаны с цифрами (высота строки, отступ) и исправлены; на каждом экране, кроме
плеера и онбординга, есть путь назад, и он проверен кликом; после правок снят скриншот
каждого исправленного экрана.

## 10. Блок «3000 частых слов» со счётчиком «знаю N из 3000» ✔

**Сделано 13.09.2026.** `en-wordlist-3000.json` — 3000 слов, у каждого часть речи,
перевод и частотный ранг. Экран «3000 слов» (вход с «Тренировки», подпись показывает
«знаю N из 3000»): общий счёт, разбивка по трём тысячам, просеивание пачками по 30 и
изучение отмеченного.

Источник: состав — открытый Oxford 3000, порядок — частотность корпуса субтитров
OpenSubtitles. Ссылка Кристиана (scribd) текст не отдаёт — страницы там картинками за
пейволом. Частотность именно по речи, а не по вебу: на корпусе Google Web в первую сотню
попадали `dvd`, `cd`, `click`, `index`, `website`, а по субтитрам `email` уезжает на
2513-е место.

Правило «слово известно» опирается на две вещи: человек сказал «знаю» при просеивании
либо вспомнил слово три раза подряд в изучении — **и** отдельно слово засчитывается, если
встречалось в упражнении, ушедшем по интервальному повторению на неделю вперёд. Последнее
и есть та половина счёта, которая не зависит от самооценки.

Проверено фактически: отметил 10 знакомых и 5 незнакомых — счётчик показал 10, «Учить
незнакомые (5)», в `localStorage` легли те же слова; после перезагрузки цифра та же.
Выборка 50 переводов проверена глазами, ошибок нет (см. журнал).


Кристиан 13.09.2026: «я бы эти 3000 слов внёс в приложение отдельной группой изучения, чтобы
можно было их учить и одновременно отслеживать, сколько из этих 3000 уже знаешь».

Источник. Ссылка, которую он дал (scribd, документ канала Volka English), текст не отдаёт:
страницы там отрендерены картинками за пейволом — проверено 13.09.2026. Поэтому список
собирается из **открытого** частотного источника (Oxford 3000 или NGSL — именно он и даёт те
самые ~95% покрытия); если Кристиан положит свой PDF или текстовый файл в папку проекта,
брать его. Ранг частоты хранить: он нужен и для порядка изучения, и для честного «первая
тысяча закрыта».

Переводы не выдумывать наугад: один основной перевод плюс часть речи, для многозначного
слова два-три значения через точку с запятой. Выборка не меньше 50 слов проверяется глазами
и результат записывается в журнал.

Отдельно продумать вход: прощёлкивать 3000 карточек, половину из которых он знает, никто не
станет. Нужно быстрое просеивание «знаю / не знаю» пачками, и учить потом только то, что не
знает. Правило «слово считается известным» записать словами в коде — и чтобы оно опиралось
не только на самооценку, но и на ответы в приложении.

**Готово, когда:** список лежит в контенте (`native/.../Languages/en/`) с ранжированием по
частоте; в приложении есть отдельная группа изучения и счётчик «знаю N из 3000» с разбивкой
по тысячам; просеивание работает пачками; счётчик проверен фактически — отметил N слов,
счётчик показывает N, после перезагрузки цифра та же; `npm test` зелёный; замер объёма и
выборка переводов записаны в журнал.

## 11. Банк контрольного среза для A2 и B2 ✔

**Сделано 13.09.2026.** `en-checkup-a2.json` (20 заданий, 15 тем) и `en-checkup-b2.json`
(20 заданий, 11 тем). Непересечение с курсом держит тот же тест, что и для B1, — и он
сразу поймал два задания A2, которые совпали с упражнениями («You should rest.»,
«If you heat water, it boils.»); оба заменены. Проверено живьём на профиле A2: секция
«Контрольный срез» появилась, первое задание открывается, счётчик 1/20.


Сейчас срез есть только для B1 (пункт 6). На других уровнях секция «Контрольный срез» просто
не показывается, то есть замерить прогресс там нечем.

**Готово, когда:** `en-checkup-a2.json` и `en-checkup-b2.json` собраны по тем же правилам —
20 заданий, не меньше 10 тем уровня, ни одного пересечения с курсом (держится тем же тестом,
что и B1).

---

## 12. Ни один режим не говорит «пока нечего» ✔

**Сделано 14.09.2026.** Правило одно и живёт в ядре (`core/modes.ts`): закрытый режим
говорит, чем он открывается. Замер `npm run measure:modes` перечисляет все режимы для
чистого прогресса на десяти уровнях двух языков — закрытых без объяснения ноль, иначе скрипт
выходит с кодом 1. Живая проверка на 390×844: десять строк, у всех высота 66 px и поля 19/19
(до правки строки скакали 66 → 77 px, потому что длинные подписи переносились на второй ряд —
ровно та жалоба, с которой начался пункт 9; тексты сокращены до 34 знаков, и это закреплено
тестом).

Заодно выяснилось, что режимов не одиннадцать, а десять: цифра в этом пункте была взята на
глаз, а замер их пересчитал.

На «Тренировке» с чистого профиля B1 три режима из десяти отвечали «пока нечего»:
«Перевод», «Тесты», «Диалог». Не потому, что контента нет — на B1 2 790 тестов и 3 216
переводов, — а потому что после правки 11.09 практика берётся только из **пройденных**
уроков. Это правильное решение (иначе на B1 снова всплывёт `hello`), но рядом стоят «Слова»,
«Формы глагола» и «Фразы», которые берут материал со всего уровня и потому не пустуют. Одно
приложение, два разных правила — и человек видит в этом поломку, а не замысел.

Решить одним правилом: что именно открывает каждый режим и как об этом сказано. Пустой режим
обязан объяснить, что его откроет («пройди первый урок блока»), а не сообщать «пока нечего».

**Готово, когда:** на чистом профиле каждого уровня EN и ES ни один режим на «Тренировке» не
показывает «пока нечего» без строки, называющей условие открытия; условие проверено скриптом,
который перечисляет режимы для пустого прогресса (а не глазами по одному экрану); `npm test`
зелёный.

## 13. Испанский получает слой разборов и срез ✔

**Сделано 14.09.2026.** `es-theory-a1.json` и `es-theory-a2.json` — по 12 разобранных тем
(49 и 47 разделов, 51 и 53 минуты чтения, 48 разобранных ошибок и 13-14 таблиц в каждом
пакете — та же плотность, что у английских). `es-checkup-a1.json` — 20 заданий вне курса,
все 12 грамматических тем A1 покрыты. Темы взяты не на глаз: грамматических тем в
`es-syllabus.json` на A1 ровно 12 и на A2 ровно 12, и разборы легли один к одному.

Проверено: `decodeTheory` и `decodeCheckup` на испанском контенте не бросают, чужих
`topicID` ноль, повторов курса в срезе ноль. Заодно починен храповик: тесты передавали
`decodeCheckup` **без курсов**, то есть правило «срез не повторяет курс» в `npm test` не
работало вовсе — теперь работает, и английские банки его проходят. Живая проверка: на
испанском A1 экран «Разборы тем» показывает все 12, «Сегодня» предлагает разбор с
практикой, таблицы и разборы ошибок рисуются.

Испанские A1 и A2 — это по 90 часов контента (6 981 и 6 870 упражнений), и при этом у
испанского **нет ни одного разбора темы, ни одного среза, ни списка слов**: всё, что сделано
11–13.09, сделано только для английского. Люди, которым Кристиан дал приложение, учат как раз
испанский — для них приложение осталось тем, на что он сам жаловался: практика без объяснений.

Сделать по образцу английского: `es-theory-a1.json` и `es-theory-a2.json` (темы привязаны к
`es-syllabus.json`, таблицы, разобранные ошибки), `es-checkup-a1.json` — срез вне курса.

**Готово, когда:** A1 и A2 испанского имеют не меньше 12 разобранных тем каждый, все `topicID`
существуют в силлабусе (иначе `decodeTheory` падает); банк среза A1 не повторяет ни одного
задания курса (это проверяет `decodeCheckup` и он обязан не бросать); выборка из 20 правил
проверена глазами и записана в журнал; `npm test` зелёный.

## 14. Испанский на равных в словах и на слух ◐

**14.09.2026: аудирование сделано, счёт словаря до критерия не дотянул.**

*Аудирование — закрыто.* У испанского появились свои правила (`core/spanish.ts` +
`SPANISH_FRAGMENT` в `listening.ts`): набор A1 вырос с «пропускаем всё» до 4 164 фраз из
4 513, и в выборке 30 фраз обрывков нет ни одного. Тест требует, чтобы в каждой фразе
набора был глагол. Попутно нашлось, что испанские **вопросы не попадали в аудирование
вовсе**: правило формы предложения начиналось с `^[A-Z]`, а испанский вопрос начинается
с «¿». Теперь попадают, и тест это держит.

*Словарь — 396 единиц на A1 вместо требуемых 800.* Было 244. Критерий 800 я взял из
английской пропорции, и он на этом контенте недостижим честно: в английском A1 одиночных
слов на карточках 750 из 2390, в испанском — 42 из 2435, курс нарезан фразами. Замерены
два правила:

| правило | единиц A1 | осмысленных в выборке |
|---|---|---|
| «нет глагола — значит группа» | 686 | 80% (8 предложений из 40) |
| «группа обязана начинаться как группа» (взято) | 396 | **92%** (46 из 50) |

Вторая половина того же критерия требует не ниже 90%, поэтому взято второе правило.
Догонять счёт до 800 можно только предложениями в словаре — то есть возвращая ровно тот
нейрослоп, из-за которого пункт 1 и появился.

**Осталось:** либо принять 396 как факт контента, либо добавить в испанские курсы
одиночные слова — это правка контента, а не правил, и она в другом пункте.

Правила отбора лексических единиц и фильтр аудирования писались под английский: испанский
словарь A1 — 244 единицы против 1 385 английских, а фильтр обрывков для испанского остался
прежним, мягким (записано открытым 12.09). То есть два режима из одиннадцати для испанского
работают хуже, и это видно по числам, а не по ощущению.

**Готово, когда:** `npm run measure:vocabulary` для ES даёт не меньше 800 единиц на A1 и долю
осмысленных не ниже 90%; `npm run measure:listening` для ES не показывает обрывков (по тем же
правилам, что у английского, но со своими списками служебных слов); выборка 50 единиц и 30
фраз проверена глазами и записана в журнал; `npm test` зелёный.

## 15. Новые экраны в общий ритм ✔

**Сделано 14.09.2026.** Замер `getBoundingClientRect` на 390×844 по четырём экранам:
поля слева и справа везде **18/18**, без исключений. Ровными оказались три из четырёх:
«3000 слов» (три строки по тысячам — по 55 px), «Формы глагола» (два поля ввода по 65 px),
«Срез» (кнопки по 48 px, карточка и поле 18/18). Два дефекта найдены числами и исправлены:

| что | было | стало |
|---|---|---|
| три ссылки внизу «Тренировки» | правый край 124 / 91 / 130 px | **18/18**, высота 48 у всех |
| список разборов, 13 карточек | 90,8 / 107,6 / 124,4 px | **107,6 px** у всех |

Ссылки были `inline-flex`, то есть шириной по тексту, — теперь это строка во всю ширину с
шевроном у правого края (шеврон пришлось вынести из текста в отдельный элемент, иначе
выравнивать нечего). Подпись разбора прижата к двум строкам: у тринадцати тем B1 она
занимала от одной до трёх, отсюда и скачки высоты.

Пункт 9 выровнял интерфейс 12.09, а экраны «3000 слов», «Срез», «Разбор темы» и «Формы
глагола» появились после него — их никто не замерял. Плюс записанная открытой мелочь: на
«Тренировке» две текстовые ссылки внизу разной длины (91 и 130 px от края).

**Готово, когда:** на 390×844 замерены `getBoundingClientRect` всех строк этих четырёх экранов;
одинаковые по смыслу строки имеют одинаковую высоту, поля слева и справа совпадают до пикселя;
замер приложен в журнал числами; `npm test` зелёный.

## 16. Обновление на телефоне без объяснений ✔

**Сделано 14.09.2026, проверено двумя настоящими сборками.** Стенд: статический сервер на
5190, в него подкладывается то, что считается «выложенной версией». Сценарий пройден
дважды, и оба поведения — те, что нужны:

| когда пришла новая версия | что сделало приложение |
|---|---|
| человек ничего не делает | **обновилось само** и перезагрузилось: было `index-B3CVdusV.js`, стало `index-DuRCBmXZ.js`, в настройки заходить не понадобилось |
| человек в середине урока | урок **не прерван**, сверху плашка «Готова новая версия — обновится, когда закончишь. Или нажми, чтобы сейчас» |

Попутно выяснилось, **почему 13.09 приходилось объяснять словами**: при первом заходе
service worker ставится, но страницей ещё не управляет (`navigator.serviceWorker.controller`
пуст), и обновления в этом состоянии ведут себя иначе — новый воркер встаёт сразу, без
ожидания и без события. На телефоне же стояла сборка, сделанная **до** появления этой
логики: её воркер обновление вообще не искал. То есть чинить было нечего — нужно было
выложить то, что уже написано.

«Проверить обновление» теперь отвечает тремя словами вместо двух: «проверяю…»,
«обновляю…» (нашлось — применяю) и «уже свежая».

Service worker стоит в режиме `prompt`, и 13.09 пришлось объяснять словами: «прими предложение
обновиться, а если его нет — нажми «Проверить обновление» в настройках». Человек, которому
приложение просто дали, такого объяснения не получит и будет пользоваться старой версией,
считая, что всё исправное.

**Готово, когда:** после выкладки новой версии установленная PWA сама предлагает обновиться при
первом открытии (без захода в настройки), и это проверено двумя разными сборками локально —
поднять одну, установить, поднять вторую, увидеть предложение; «Проверить обновление» отвечает
словами, что именно произошло («уже свежая» / «обновляю»); `npm test` зелёный.

## 17. Больше форматов и картинки — как в Duolingo ◐

Кристиан 14.09.2026, посмотрев Duolingo: «много интерактива, много заданий с визуалами, с
карточками, я думаю нам следует тоже самое добавить».

Суть не в контенте, а в **форме вопроса**. Duolingo показывает одно и то же слово пятью
способами, и именно это держит внимание. У нас про это есть отложенный пункт («урок B1 —
это один диалог и тринадцать упражнений про те же четыре фразы»), и форматы закрывают его,
не переписывая тридцать семь тысяч упражнений: новый формат собирается из того, что уже
лежит в курсе.

**Сделано 14.09.2026 — два формата, и оба из готового материала:**

- **«Найди пару»** — шесть слов и шесть переводов, соединить нажатиями. Материал общий с
  режимом слов, поэтому в парах только лексические единицы. Набор всегда решаем: ни
  одинаковых слов, ни одинаковых переводов внутри набора (в курсе есть «la próxima» и
  «la próxima vez» с одним переводом — они встретились бы неизбежно).
- **«Что ты слышишь»** — фраза звучит, на экране четыре варианта. Ступенька между «повтори
  вслух» и «запиши, что услышал». Приманки подобраны по близости к ответу (общие слова,
  похожая длина), иначе задание решалось бы по длине строки. Звук уже есть — 37 248
  записей, и формат окупает те 262 МБ.

На «Тренировке» стало **12 режимов вместо 10**, на свежем B1 открыто 8 (было 6).

- **«Выбери картинку»** — слово и четыре картинки, сетка два на два. Набор OpenMoji
  (CC BY-SA 4.0): 222 файла, 1,1 МБ, лежат в сборке и работают офлайн. Карта «слово →
  картинка» строится скриптом `build-pictures.mjs` и покрывает 248 существительных.

Правило карты жёсткое: **неверная картинка хуже отсутствующей**, потому что учит не тому
слову и человек этого не замечает. Свободные метки OpenMoji давали 1117 слов из 3000, но
среди них «cancer → 🦀 краб», «pool → 👙 бикини», «summer → 🍺 кружка пива» — поэтому
принимаются только точные совпадения имени, а остальное размечено руками и проверяется
по имени картинки: опечатка валит сборку, а не уезжает в приложение.

На «Тренировке» стало **13 режимов**, на A1 открыто 8.

**Осталось по этому пункту:**

1. Картинки для испанского: карта строится от английского списка 3000, испанскому нужна
   своя — либо через русский перевод, который у обоих языков общий.
2. Новые форматы внутри урока, а не только в «Тренировке» — это правка лестницы шагов
   (диалог → карточки → правило → узнавание → производство) и храповика к ней.
3. «Собери, что слышишь» — звук плюс банк слов. Банк слов уже есть (см. ниже), звук тоже,
   формат — это их сложение.

**Пункт, которого не понадобилось.** «Собери из слов банком, как в Duolingo» я записал в
план, не посмотрев на экран: `WordOrderTray` ровно такой и есть — поднос для собранной
фразы и слова-кнопки под ним, нажатием туда и обратно. Вычеркнуто по факту, а не сделано.

**Картинка на карточке слова — сделано 14.09.2026.** Та же карта, что в формате: на
карточке «hotel» появляется 🏨, на «a cat» — 🐈 (артикль снимается). У большинства карточек
картинки нет, и тогда не рисуется ничего: пустая рамка хуже отсутствия картинки.

**Готово, когда:** в «Тренировке» не меньше четырёх новых форматов; каждый собирается из
существующего контента и держится тестом на решаемость (нет неразрешимых наборов, нет
вариантов-двойников); картинки лежат в сборке и работают офлайн; `npm test` зелёный.

## 18. Новые форматы внутри урока, а не только в «Тренировке» ✔

**Сделано 15.09.2026.** Шаг формата собирается на ходу из карточек того же урока
(`core/lessonformats.ts`) — контент не тронут, `id` не сдвинулись. Замер: формат достаётся
**64% уроков A1** (картинки 136, пары 195, на слух 126), 44% A2, 27% B1; у испанского A1 —
63%. Храповик `npm run check:ids` сверяет 3 883 урока и 52 093 упражнения с отпечатком и
падает на любом расхождении — проверен подменой одного `id`: упал и назвал урок.

Живьём: в уроке A1 «Hello!» шаг «Найди пару» встал сразу после карточек, собрался и урок
пошёл дальше; в уроке B1 «Формы, которые не меняются» встали **два** шага — «Найди пару» и
«Что ты слышишь», после них урок продолжился. Шаг картинок проверен отдельным режимом и
тестами (живьём в уроке не дошёл: нужные уроки заперты порядком курса).

Три новых формата (пары, «что слышишь», картинки) живут на «Тренировке», а время человек
проводит в уроке: «Следующий урок» на «Сегодня» — главная кнопка приложения. Пока форматы
стоят рядом с уроком, а не внутри него, однообразие урока никуда не ушло: один диалог и
тринадцать упражнений про те же четыре фразы.

Урок обязан остаться лестницей (диалог → карточки → правило → узнавание → производство), и
это держит храповик. Новый формат — это **другой способ задать тот же шаг**, а не новый шаг:
карточка может показаться парами или картинкой, узнавание — выбором на слух.

**Главное ограничение: `id` уроков и упражнений менять нельзя.** На них стоит весь прогресс
и повторение; сдвиг `id` обнулит людям месяцы.

**Готово, когда:** в уроке на A1 и на B1 встречается не меньше двух новых форматов;
замер показывает, сколько шагов урока приходится на каждый формат; скрипт сверяет `id`
упражнений и уроков с прежним списком и падает на любом расхождении; `npm test` зелёный.

## 19. Картинки для испанского ◐ — сделано, но не через русский перевод

**15.09.2026: картинки у испанского есть, идея с русским мостом не подтвердилась.**

Мост работает как замысел, но не как инструмент: он нашёл всего **пять** слов, и **два из
них соврали** — «brazo — рука» получил ✋ ладонь (рука ≠ ладонь), «espacio — место» — 💺
сиденье. Сорок процентов ошибок против правила «неверная картинка хуже отсутствующей» не
проходят, поэтому мост выключен (код и объяснение остались в скрипте), а три верные находки
перенесены в ручную разметку. Причина слабости моста измерена: русские подписи у двух курсов
сформулированы по-разному, точных совпадений почти нет.

Сделано иначе: **73 испанских слова размечены руками** — не из головы, а из 343 одиночных
слов, которые в курсах A1 и A2 действительно есть (иначе карта была бы мёртвой: «el perro →
🐕» бесполезно, если курс никогда не говорит «perro»). Имена картинок проверяются сборкой:
две мои опечатки и одно слово, которого нет в курсе, она поймала и не дала записать.

Вычитаны **все 82 записи**, а не выборка 40: шесть врали и убраны («cola → 👯 люди с ушами»,
«cuenta → 💸 улетающие деньги», «tostadora → 🍞 хлеб», «norte» и «sur» получали одну и ту же
🧭 — в вопросе это два верных ответа), ещё три убраны как сомнительные.

**Критерий «не меньше 120 слов на A1» не выполнен: 73 слова всего, из них на A1 доступно 11,
на A2 — 74.** Причина в контенте: испанский A1 — это приветствия и фразы, одиночных
существительных там почти нет. Добить до 120 можно только словами, которых в курсе нет.

Живьём: режим «Выбери картинку» на испанском A2 показывает «el puesto» и четыре загруженные
картинки; шаг картинок попадает в 11 уроков A1 и 72 урока A2. Храповик «у каждой картинки
есть файл» теперь держит оба языка.

Карта картинок построена от английского списка 3000, поэтому у испанского картинок нет ни
одной. Но перевод у обоих языков **общий**: карточка «el perro — собака» и слово «dog —
собака» указывают на одну картинку 🐕. То есть карту можно строить не от слова, а от
русского перевода, и она сразу заработает для второго языка.

Осторожно с многозначностью: «light — фонарь» и «a light — свет» уже показывают, что один
русский перевод не всегда один предмет. Неверная картинка хуже отсутствующей.

**Готово, когда:** у испанского A1 не меньше 120 слов с картинкой; выборка 40 проверена
глазами и неверных в ней не больше двух (записать в журнал числом); храповик «у каждой
картинки есть файл» держит оба языка; `npm test` зелёный.

## 20. «Собери, что слышишь» — четвёртый новый формат ✔

**Сделано 15.09.2026.** Фраза звучит, внизу слова — из них надо собрать порядок. Банк слов
не написан заново: это тот же `WordOrderTray`, что в упражнениях курса. Приманки (до трёх)
берутся из слов соседних фраз набора и **никогда не повторяют слово фразы** — иначе у банка
было бы два верных порядка, и приложение назвало бы ошибкой то, что ошибкой не является;
это держит тест. Берутся фразы от трёх до девяти слов: короче собирать нечего, длиннее —
банк не влезает в экран.

Формат есть и на «Тренировке» (14 режимов вместо 13), и шагом внутри урока. С ним доля
уроков с новым форматом выросла: **87% на EN A1** (было 64%), 85% A2, 83% B1, 92% на ES A1.

Живьём: фраза играет из спрайта (перемотка 14,828 → 15,298 с), банк — 8 слов на фразу из
пяти, собранный неверный порядок даёт «Вот что там было» с самой фразой, «Дальше» ведёт к
следующей.

Звук плюс банк слов: фраза звучит, внизу слова, из которых её надо собрать. В Duolingo это
основное упражнение на слух, и у нас для него уже есть всё — озвучка на 37 248 фраз и
готовый банк слов (`WordOrderTray`). Это ступенька выше, чем «выбери вариант»: слова даны,
но порядок надо услышать.

Приманки в банке нужны, иначе задание решается без слуха: собрать можно только одним
способом. Лишние слова брать из других фраз уровня, не из воздуха.

**Готово, когда:** формат есть и на «Тренировке», и в уроке; банк содержит все слова фразы
плюс 2-3 приманки без повторов; ответ проверяется тем же сличением, что переводы; тест на
решаемость (банк собирается в фразу ровно один раз); `npm test` зелёный.

## 21. Картинки в списке 3000 и на просеивании ✔

**Сделано 15.09.2026.** На карточке просеивания появилась картинка слова — 112×112, крупнее
чем в уроке: решение «знаю / не знаю» принимается за секунду, и картинка должна читаться
боковым зрением. Где картинки нет, не рисуется ничего.

Проверено прощёлкиванием 360 слов подряд: картинка выпала у **40** из них («time ⏰»,
«man 👨», «day ☀️», «home 🏠», «night 🌃», «money 💰», «girl 👧»), все файлы загрузились, все
112×112. Первые шестнадцать слов списка идут без картинок — и это верно: по частоте там
«you», «the», «to», а не предметы.

Замер строк сводки на 390×844: три строки по тысячам — по **55 px**, поля **18/18**, то есть
ритм не поехал.

Экран «3000 частых слов» — то место, где Кристиан отмечает «знаю / не знаю» пачками. Слово
с картинкой просеивается быстрее, чем слово текстом, и 248 картинок уже лежат в сборке.

**Готово, когда:** на карточке просеивания и в списке слово с картинкой её показывает, а
слово без картинки не оставляет пустой рамки; замер на 390×844 подтверждает, что высота
строк списка не поехала (числа в журнал); `npm test` зелёный.

## 22. Список частых слов для испанского ✔

У испанского не было списка частых слов, и из этого следовало три вещи: не было режима
просеивания «знаю / не знаю» (тот, которым Кристиан пользуется на английском), картинок
было 73 против 248 английских, а словарь режима «Слова» упирался в курс, написанный
фразами. Пункты 14 и 19 оба встали на этом.

**Сделано 15.09.2026: 826 слов.** Порядок — частотность по корпусу субтитров
OpenSubtitles 2018 (тот же источник, что у английского списка). Значения — **Русский
Викисловарь** (CC BY-SA): испанское слово, русский перевод, часть речи. Ничего не
выдумано: слова, которых Викисловарь не знает, в список не попали (из 1500 частых
получилось 837, после вычитки 826).

Что пришлось решать руками — и это выбор из написанного в источнике, а не свой перевод:

- **102 слова-омонима** с несколькими испанскими блоками: «amigo» шёл прилагательным
  «дружественный» вместо «друг», «bien» — существительным «благо» вместо «хорошо»,
  «este» — «восток» вместо «этот». Для 21 слова выбран другой блок.
- **три слова выброшены**, потому что нужного смысла в Викисловаре нет вовсе: «mira»
  (только «прицел», а в речи это «смотри»), «vale» (только «вексель», а это «ладно»),
  «dicho» (только «пословица»).
- **пять бранных слов** убраны: корпус субтитров их даёт часто (mierda на 137-м месте),
  но тренажёр дают людям, и учить их этому незачем.
- у «luego», «claro», «haber» взято второе значение блока: первое там книжное.

**Проверка независимая, а не на глаз:** 52 слова списка встречаются и в самом курсе со
своим переводом. Совпало по смыслу 30, остальные 22 — многозначность, а не ошибки
(«estación» у Викисловаря «время года», в курсе «вокзал»; «razón» — «разум» и «причина»).
Ни одного неверного перевода сверка не нашла.

**Следствия, которых и добивались:**

| | было | стало |
|---|---|---|
| режим просеивания у испанского | нет | **826 слов**, пачками |
| испанских слов с картинкой | 73 | **178** (мост через русский заработал) |
| картинок на карточках списка | — | 115 из 826 (14%; у английского 8%) |

Мост через русский перевод, который 14.09 не подтвердился, теперь работает: тогда он
сравнивал словарное значение с подписью карточки курса («Пришлю расписание»), а теперь —
два словарных значения. 126 находок, все вычитаны, 108 оставлены.

Заодно убрано вбитое в интерфейс «3000»: название и объём берутся из пакета, иначе у
испанского в подписи стояло бы «3000 частых слов: знаю 0 из 826».

**Проверено живьём:** на «Тренировке» испанского появилась строка «826 частых слов: знаю 0
из 826», просеивание идёт пачками, 300 слов прощёлкано подряд — у 39 из них картинка, все
загрузились.

**Озвучка списка: 826 × 2 файла, 3,4 МБ.** Те же дикторы, что читают испанские фразы
(es_ES-davefx и es_MX-claude), иначе слово и фраза звучали бы разными голосами. Попутно
нашлись два тихих сбоя в генераторе, и оба чинились не в контенте:

- `build-voice.mjs` кормил Piper словами через `input` в `promisify(execFile)`, а такого
  параметра у `execFile` нет — он молча игнорируется. Piper поднимал модель и засыпал на
  пустом stdin: прогон провисел 14 минут, израсходовав 0,6 секунды процессорного времени и
  не создав ни одного файла. Это касалось и английского — то есть `npm run build:voice` в
  исходном виде не сработал бы ни на одном языке. Теперь `spawn` и закрытый вручную stdin.
- пропал предлог `con`: Piper прогоняет имя через `pathvalidate`, а тот дописывает
  подчёркивание к зарезервированным в Windows именам (CON, PRN, AUX, NUL, COM1). Вышло
  `con_.wav`, сжатие его не нашло, и одно из самых частых слов молча читалось бы системным
  голосом. На класс ошибки поставлен храповик: у каждого слова списка обязаны быть оба файла.
  Проверен подменой — файл убран, тест назвал именно `voice/es/male/con.opus`.

Правило имени файла переехало в ядро (`core/voicefiles.ts`): три копии расходятся молча,
потому что несовпадение имени не ломает сборку, а тихо уводит слово в системный синтез.

**Проверено живьём:** кнопка 🔊 на карточке `país` запросила `voice/es/female/país.opus`,
`currentTime` сдвинулся с 0 до 0,224 при длительности 0,564, системный синтез не вызывался.
Тесты: 192 зелёных.

## Отложено сознательно

**Английский C1 — заглушка.** 89 упражнений против 13 461 на B1 (48 КБ против 7,4 МБ).
Наполнение — это работа на много проходов, как B2, и Кристиан на B1: ему C1 не нужен. Уровень
уже честно помечен в интерфейсе как несуществующий (коммит «Не звать на уровень, которого ещё
нет»). Браться, когда понадобится, а не «для полноты».

**Шаблон урока.** Урок B1 — это один диалог из четырёх реплик и 13 упражнений про те же
четыре фразы; так сделаны все ~1000 уроков B1. Именно это делает уроки однообразными. Решение
от 11.09.2026 — сначала слой разборов поверх, перегенерация только если слой не помог. Браться
не раньше, чем появится подтверждение с живого использования: это работа на много проходов, и
она сдвинет `id` уроков, то есть прогресс.
